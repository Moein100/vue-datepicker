export const TIME_FORMATS = Object.freeze({
  TWELVE_HOUR: 12,
  TWENTY_FOUR_HOUR: 24,
});
export const TIME_PERIOD = Object.freeze({
  AM: 'AM',
  PM: 'PM',
});

export const TEXT_DECORATIONS = Object.freeze({
  RTL: 'rtl',
  LTR: 'ltr',
});

export const DATE_FORMATS = Object.freeze({
  STANDARD: 'YYYY/MM/DD',
  DASHED: 'YYYY-MM-DD',
  DOTTED: 'YYYY.MM.DD',
});

export const SELECTION_MODES = Object.freeze({
  SINGLE: 'single',
  RANGE: 'range',
  MULTIPLE: 'multiple',
});

export const VIEW_MODES = Object.freeze({
  DAYS: 'days',
  MONTHS: 'months',
  YEARS: 'years',
});

export const CALENDAR_CONFIG = Object.freeze({
  DAYS_IN_WEEK: 7,
  TOTAL_CELLS: 35,
  YEAR_RANGE_OFFSET: 4,
  YEARS_TO_SHOW: 12,
  MONTHS_IN_YEAR: 12,
});

export const KEYBOARD_KEYS = Object.freeze({
  ARROW_LEFT: 'ArrowLeft',
  ARROW_RIGHT: 'ArrowRight',
  ARROW_UP: 'ArrowUp',
  ARROW_DOWN: 'ArrowDown',
  ENTER: 'Enter',
  SPACE: ' ',
  ESCAPE: 'Escape',
  TAB: 'Tab',
});
